<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>함께사는양평 - AI 사회권 보호 시스템</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; font-family: 'Pretendard', sans-serif; }
        .navbar { background-color: #27ae60 !important; }
        .card { border-radius: 15px; border: none; box-shadow: 0 4px 6px rgba(0,0,0,0.1); transition: 0.3s; }
        .score-badge { font-size: 0.9rem; font-weight: bold; padding: 5px 12px; border-radius: 20px; }
        .bg-safe { background-color: #e8f5e9; color: #2e7d32; }
        .bg-danger-custom { background-color: #ffebee; color: #c62828; }
        .btn-yp { background-color: #27ae60; color: white; border-radius: 8px; }
        .btn-yp:hover { background-color: #219150; color: white; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">🌳 함께사는양평</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">홈</a>
                <a class="nav-link" href="/admin">AI 관제센터</a>
            </div>
        </div>
    </nav>
    <div class="container">
        {% block content %}{% endblock %}
    </div>
</body>
</html>